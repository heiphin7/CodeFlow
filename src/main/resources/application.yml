spring:
  application:
    name: codeflow
  datasource:
    url: jdbc:postgresql://localhost:5432/codeflow
    username: postgres
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
server:
  port:
    9273